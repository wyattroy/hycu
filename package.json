{
  "name": "hycudesign.com",
  "private": true,
  "description": "Hycu studio site. Static, no build step; GitHub Pages serves the repo root.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8787 --bind 127.0.0.1",
    "check": "node scripts/check.mjs",
    "shoot": "node scripts/shoot.mjs",
    "test": "node scripts/check.mjs && node scripts/shoot.mjs",
    "edit": "node scripts/edit-server.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
