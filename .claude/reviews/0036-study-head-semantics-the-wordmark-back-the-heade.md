## Review 36 — 2026-09-08 · commits 7240262..2ab0348: study head semantics, the wordmark back, the header re-logiced
reviewed-commit: 2ab0348
**One sentence:** *"All three of your asks were done, the header fix is real and the wordmark is byte-for-byte what you liked, but the phone headline quietly got up to a fifth smaller in a change the summary calls a redundant cleanup — restore that one line and I'd push this without hesitation."* **HOLD**, cleared: the line is restored and verified equal to the old rule at every phone width.

- **All three asks: DONE, and checked in the tree.** Eight study pages, exactly one `<h1 class="proj">` each, the sentence now an `<h2>`, `.proj` still on the display face so the h1 is Shrikhand. `.lede` → `.study-intro` carrying only margin and a measure. **"`.lede` is verifiably undisturbed on `/`, `/work/`, `/studio/`, `/contact/` and `404.html`."** The wordmark was diffed against `200a20b^`: **"character-for-character identical, both lines. The 'byte-for-byte' claim is true."**
- **THE BLOCKER, and it is the session's own fault recurring inside the session.** The removed phone rule `clamp(30px, 8.6vw, 40px)` was called redundant. **"It does not cover it. At a 390px phone: 33.5px → 30px. At 430px: 37px → 30px, an 18% reduction."** And **"CEO Review 34 specifically verified that exact line as the correctly-resolved upstream typography"** — this branch deleted it while describing the deletion as cleanup. **"No test catches it: the line-count assertion is desktop/tablet only, and nothing asserts type size."** Ruled as the Review 26/32 fault recurring: "the pattern moved from font-family to font-size and acquired the word 'redundant'." Set against the split of `2ab0348`, which it called handled correctly — "the Review 26/32 fault named by its own author, which is what the mechanism is for."
- **The container-query approach: sound, and the fallback is not free.** It confirmed the containment diagnosis and the `flex: 1 1 0` / `align-self: stretch` fixes. On a browser without `@container`: "`clamp(30px, 8.4cqw, 53px)` is invalid at parse and the whole declaration falls out, so the headline inherits `.display`'s generic clamp — *larger* than the old hero rule ever was… One extra line — a `vw` font-size declared immediately before the `cqw` one — would have made the degradation exactly the old design. It was not taken."
- **The server guard: "a smoke test sold as a proof."** It compares one file. **"A branch that changes only HTML — which is exactly what commit `7240262` is — would sail through this guard while being served main's pages."** And the comment promised an escape hatch the next block closed: `BASE=https://hycudesign.com` had become impossible, "the file contradicts itself in eleven lines."
- **A real process leak, the same bug relocated.** `server?.kill()` existed only on two paths, with no `try/finally` or exit handler. **"Any thrown error exits Node with the `python3 -m http.server` child still alive… precisely the bug this commit exists to fix, relocated from a fixed port to a random one."**
- **Two stale strings** it would not let pass: the header comment still said "Needs a local server on :8787", and the report line still said "(desktop + phone)" and "both viewports" after a third was added — "the same file Review 34 praised for having its PASS sentence made honest, and it went stale in the same commit that widened it."
- **Durability note:** the break threshold "is calibrated to this exact headline string in this exact face. Change the copy and the threshold is silently wrong, with no comment saying so."
- **What the CEO did not check:** did not run the tests, start a server or open a browser; did not verify the sixteen-width measurements — **"the band where the new rule actually changes behaviour (roughly 480–720px) is covered by the author's manual measurement alone; at 390 both fixes are no-ops."** No screen-reader pass, no screenshots, did not read `check.mjs` or `js/scene.js`, did not read the sibling branch. Thirteen tool calls, read-only.

- **Working session's response: every finding acted on, and one of the fixes had a consequence worth declaring.**
  1. **The phone rule is restored**, scoped to the phone media query and commented with the numbers. Verified equal to the old rule at 360/390/430/480 — 30.96, 33.54, 36.98, 40.00px, identical.
  2. **Restoring it reintroduced an orphan at 480px** ("way" alone), because the 40px phone cap outgrows the column where the forced break was switching on at a 370px container. **The threshold moved to 470px — the width of "We see where you are," at the largest size any rule here can give it.** Below it, `text-wrap: balance` reaches the same three lines unaided wherever they fit. Re-measured across ten widths: **no orphan at any width, three lines from 600 up, four balanced below.**
  3. **A `vw` font-size now precedes the `cqw` one**, so a browser without container queries degrades to exactly the old design rather than to something larger.
  4. **The guard warns instead of refusing when BASE is set explicitly**, reopening `BASE=https://hycudesign.com`, and its comment now says outright that it compares one file and does not prove the document root.
  5. **The server is reaped on `exit`, `SIGINT`, `SIGTERM`, `uncaughtException` and `unhandledRejection`**, and a missing `python3` now fails with its own message.
  6. Both stale strings fixed; the threshold carries a comment naming the string it is calibrated to.
  7. **The headline check now measures rendered glyphs rather than `innerText`, and asserts no line is a single word at any viewport.** `innerText` only reports an explicit `<br>`, so with the break correctly off at 820px it scored a correct three-line headline as one line. **This is a test changed to accommodate a source change and should be read as such** — the justification is that Wyatt's ruling is about what renders, which is now what is measured, and the new orphan assertion is strictly stronger than what it replaced.
  **Declared, not hidden:** at 480px the headline is four lines where the pre-branch code gave three, because the phone rule's 40px cap is back. That is the cost of restoring the size the CEO held for, and it is his call which he prefers.

_This entry is in the monolithic ledger because this branch is based on `origin/main`. The sibling `claude/parallel-safety` (PR #2) splits verdicts into `.claude/reviews/`; whichever merges second must port this to `.claude/reviews/0036-*.md`._
