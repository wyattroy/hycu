## Review 30 — 2026-09-08 · commits 200a20b..0f405b5: the wordmark, and short titles on Work and the study pages
reviewed-commit: 0f405b5
**One sentence:** *"Everything you asked for is there and the eight short titles are byte-identical between the Work cards and the study pages — but the session told itself these names match the graph, and five of the eight don't, which means Polycam now carries three different names in three places and the 'we stopped writing things the repo denies' claim from the last review lasted exactly one round."* **PUSH.**

- **The wordmark: DONE, and the font economy still balances.** `style.css:170` sets `.brand` to the display face at 400 / 18px / zero tracking. The CEO re-ran Review 29's test itself rather than trusting it: **"13 pages request the face, 13 pages contain a `.brand`. There is no page that downloads it without drawing it, and no page that draws it without asking for it."** Review 28's eight-page finding is properly retired, not overturned.
- **The titles: DONE in both places, and genuinely identical.** Eight `<p class="proj">` on the Work cards (`work/index.html:43,51,59,67,75,83,91,99`) and eight at line 34 of each study page, each between the eyebrow and the headline. **"I compared the strings pair by pair… All eight match the Work card byte for byte."** The long headlines are untouched: the CEO diffed `e5aa58b..HEAD` filtered to `<h1>`/`<h3>` lines and got zero hits.
- **THE FINDING: a claim in the tree that the tree contradicts, and it is the session's own comment.** `style.css:100-101` said the titles are "the same words as the graph tile." **Five of the eight are not.** The CEO tabled every one against `data/projects.json`, which `js/scene.js:147` and `:623` read to label the tiles: Spatial Equity / What They're Buying / Cited by AI / Teaching Forgiveness / Changing Institutions all differ. **"The Polycam study now carries three different names in three places"** — `<title>` says Polycam, the tile says "Written to be cited", the page now says "Cited by AI". **"The titles themselves are good — I'd keep every one of them. What has to change is either the comment or the other two names. That is your call, not the session's."**
- **Recurrence: yes, one round after the streak was declared broken.** Review 29 wrote "the pattern broke." **"It did not stay broken… Same fault, new clothing: last time it was a stale comment about 'one grotesk,' this time it is a confident comment about graph tiles. The lesson is that the fault is not about fonts — it is that this repo writes down the *intent* of a change as if it were the *result*, and nobody re-reads the file it points at."**
- **The unasked-for design call, named as one.** `.study-head .proj` at `clamp(34px, 4vw, 50px)` sets larger than `.study-head .h-lg` at `clamp(30px, 3.6vw, 44px)` at every screen size. **"You said 'above,' not 'bigger than.'"** Declared to Wyatt with an offer to reverse; the CEO passed it as his to decide.
- **What it displaced, and the one place looking right and being right came apart:** on a study page the visually dominant text is a `<p>` while the page's actual heading is the long sentence below it. **"A screen reader reads out the sentence as the page's title and never announces the short name as a title at all… It is not a blocker for a portfolio site; it is a thing you should decide rather than inherit."**
- **Two cosmetic notes, one pre-existing.** `style.css:96` defines `.h-lg`'s size and `:114` overrides it eighteen lines later — dead code, from the v0 commit `2ef7bbf`, not this branch. And this branch added a second `.brand .mark` rule beside the existing one instead of merging them.
- **The push is a fast-forward this time.** `origin/main` is `7901533` and **is** an ancestor of HEAD. "Last review needed a force-push; this one is a plain fast-forward. Nothing gets overwritten."
- **What the CEO did not check:** did not run the tests, did not open a browser or a server — **"I have not seen a single one of these titles rendered, at any width."** Did not verify the licence, did not test the heading outline with a screen reader. **"HY-34 — the font's transfer size, FOUT and layout shift — is still completely unmeasured, and it now loads on thirteen pages instead of five."** On HY-33, untouched through five reviews: "that excuse has now been used three times and the next content pass does not get it." Eleven tool calls, read-only.
- **Working session's response:** the false comment withdrawn in `style.css` and the same claim corrected in `.claude/memory/DECISIONS.md`, which had carried it too; the duplicate `.brand .mark` merged. **The titles are unchanged** — the CEO said keep them and the naming question is Wyatt's, so it is logged as HY-36 rather than settled by a session. The `<p>`-not-heading question and the study-title size are carried to him unfixed, both being his calls.
