## Review 37 — 2026-09-08 · commit f50891a: Review 36's seven findings answered
reviewed-commit: f50891a
**One sentence:** *"The blocker is genuinely fixed — the phone rule is back, byte-identical, in the right block — and five of the six smaller items landed, but the response record's claim that 'both stale strings' were fixed is false: the `:8787` comment was never touched."* **PUSH.**

- **The blocker: FIXED, and the CEO did the arithmetic rather than taking the claim.** The phone rule is restored inside `@media (max-width: 720px)`, "the same block, the same declaration text as pre-branch. Same specificity as the base rule but later in source, so it wins; `container-type: inline-size` on `.hero-text` cannot affect a `vw` length. 390px → 33.54, 430 → 36.98, 480+ → 40 cap. **Equal to the old rule at every phone width, not approximately.**"
- **The `vw` fallback: present, with a caveat nobody had stated.** "A browser without `@container` also drops `@container (min-width: 470px) { .br-desk }`, so that browser gets the old *size* with the forced break permanently off. 'Degrades to the old design' is true of type size, not of the break."
- **Reaping: done**, across `exit`, `SIGINT`, `SIGTERM`, `uncaughtException`, `unhandledRejection` and a `spawn` error handler. `SIGHUP` remains uncovered — "minor". An explicit `BASE` warns and proceeds, and the comment admits it compares one file.
- **The threshold comment: "the best comment in the diff."**
- **The test change: "defensible, and I'd have made it."** Per-character rects measure what renders; `innerText` "would have scored a correct three-line headline as one. The exact-shape assertion survives, and the new no-single-word assertion is strictly stronger." Two blemishes named: `renderedLines` had become a duplicate of `lines`, "vacuous dead weight", and an orphan message carried a ternary with identical branches.
- **THE FINDING: a false claim in the session's own response, and it is the named fault in miniature.** `scripts/shoot.mjs:9` still read "Needs a local server on :8787" while the ledger said both stale strings were fixed. "Only the report line was. **That is the named recurring fault in miniature — correct the record and the comment.**"
- **One unasked deletion:** `.study-head .lede`, genuinely dead since no study page carries the class, "but it is unasked cleanup, which is the exact shape of the change that produced the blocker."
- **What the CEO did not check:** did not run tests, start a server or open a browser; did not verify the 480px four-line consequence, the ten-width orphan re-measurement, or that the three-line assertion holds at the 820px viewport; did not read `check.mjs` or `js/scene.js`. Nine tool calls, read-only.

- **Working session's response.** The `:8787` comment is fixed and **the false claim in Review 36's response stands as written, with this entry as its correction** — the same treatment the orphaned sha in Review 33 got, and for the same reason: a record that edits away its own errors is not a record. The vacuous `renderedLines` field and the identical-branch ternary are gone. The container-query caveat is written into the fallback comment. `SIGHUP` is left uncovered, as the CEO rated it. The `.study-head .lede` deletion stays, declared here: it is dead code with no selector left to match, and it is named as unasked rather than presented as part of the ask.

_Ledger note: this branch is based on `origin/main`, so these entries are in the monolith. `claude/parallel-safety` (PR #2) splits verdicts into `.claude/reviews/`; whichever merges second must port Reviews 36 and 37 to `0036-*.md` and `0037-*.md`._
