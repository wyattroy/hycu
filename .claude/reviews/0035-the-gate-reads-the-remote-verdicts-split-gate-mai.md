## Review 35 — 2026-09-08 · commits 94f6f2f..000c809: the gate reads the remote, verdicts split, gate main only
reviewed-commit: 000c809
**One sentence:** *"He asked you to turn the reviews off and you turned the volume down instead — that is the right call and the work is honest, but the new hook checks the wrong commit when you push main from a worktree, and none of it is switched on for anyone yet."* **PUSH.**

- **All three of Wyatt's asks: DONE, and the CEO ran the loop itself against six kinds of push.** Feature branch alone → ungated; main among several refs → gated; tag push → ungated. "Correct on all three." The exemption regex was run against a mixed file list: `check.mjs`, `shoot.mjs`, `style.css`, `work/*.html`, `js/scene.js`, `index.html` all stay gated. It also checked something the session had not: **`.nojekyll` exists at the repo root, so top-level Markdown is not served as a web page — the docs exemption does not quietly exempt shipped content.**
- **THE BLOCKING-CLASS BUG, found by reading the loop rather than trusting the tests around it.** The first version captured the local sha git hands it and then discarded it, checking `git rev-parse HEAD` instead. **"`git push origin main` from a feature worktree therefore reviews the feature branch and lets main through unexamined. This repo runs from three worktrees; this is not theoretical."** Two more from the same shape: a delete-push of main engaged the gate and then decided whether production could be erased by looking at an unrelated HEAD; and a final stdin line with no trailing newline left `GATED=no`, failing open — "the direction is backwards from the rest of the file", which fails closed on purpose.
- **The exemption that can remove the gate.** `^scripts/hooks/` is exempt, so "a session can weaken or delete the gate and ship that change to main with nobody reading it. The comment says this is deliberate; it is still the one exemption that can remove the gate." Pre-existing, not introduced here. Logged as HY-39.
- **All 34 verdicts survived, verified independently** by set-comparing the split files against `origin/main`'s monolith. "The only monolith lines missing are three preamble lines, replaced by fuller instructions. The only lines added are the two porting notes. Nothing else moved." Reviews 33 and 34 are verbatim, heading included, with the dead-sha correction in a footnote — "the append-only rule applied correctly". The index matched all 34 rows exactly.
- **Loosening the gate is defensible.** "You asked to switch the reviews off; what you got gates fewer *pushes*, not fewer *things*. Nothing shippable slipped out of scope — `CNAME`, `robots.txt`, `sitemap.xml`, `package.json`, `check.mjs`, `shoot.mjs` are all still covered."
- **None of it is live, and the CEO confirmed it rather than taking the claim.** `core.hooksPath` is the absolute path into the main checkout and "that installed file *differs* from this branch's copy. Pushing from this worktree right now still runs the old gate-everything hook."
- **One echo of this branch's own fault:** the brief declared build stamp `ba9e051` while HEAD was `000c809`. That commit touched only OFFICERS.md and the test run post-dates it, so nothing testable changed — "a small echo of the exact fault this branch was written to kill."
- **What the CEO did not check:** did not run `npm test`, did not open a browser or server, did not execute the hook end to end (it fetches), did not read reviews 1–32 individually beyond the whole-text comparison, did not read the installed hook in full, did not inspect the other worktrees, did not look at a rendered page, did not check branch protection or CI. Eight tool calls, read-only.

- **Working session's response, and the part to read sceptically.** All three hook bugs fixed after this verdict: the gated ref's own sha is used instead of `git rev-parse HEAD`; a delete-push of main is refused outright; `|| [ -n "$_lref" ]` closes the no-trailing-newline fail-open; and the error message now names the sha being pushed rather than local HEAD. Verified by feeding the hook five push shapes — feature-only (ungated), main at its reviewed sha (passes), main at an unreviewed feature sha (blocks, which is the bug), delete (refused), and main among several refs with no trailing newline (blocks). The index nit is fixed: row 33 now marks the sha as orphaned, a listing being free to annotate where a verdict is not. **No officer has seen any of these fixes.** They ship under the `scripts/hooks/` exemption — the very hole this verdict names — which is defensible only because they strengthen the gate rather than weaken it, and is disclosed here rather than left for the next officer to find. HY-39 covers closing that hole.
