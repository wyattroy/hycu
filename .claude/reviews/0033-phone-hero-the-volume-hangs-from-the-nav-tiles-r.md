## Review 33 — 2026-09-08 · commits ad21dce..cfa73a0: Phone hero — the volume hangs from the nav, tiles readable, no hover, no zoom buttons
reviewed-commit: cfa73a0
**One sentence:** *"Seven of your eight asks landed and the phone hero is genuinely better, but the eighth — the screenshot you called terrible — was closed as 'not our fault' without anyone asking you how wide that window was, and the summary you were handed contains two claims this repo's own files contradict, including a collision-avoidance routine it says it deleted that never existed."* **HOLD.**

- **Ask 1, graph fills the space with the title underneath: DONE, but read against ask 2, not literally.** `style.css:225` sets the phone stage to `calc(75vw + 5px)`. On a 390-wide phone that is 297px — about a third of the screen, not "the whole space." Wyatt's second message replaced the first, and that is what shipped, so this is the right call. "But you should look and say out loud whether a third of the screen is what you meant by 'whole space,' because nobody asked you."
- **Ask 2, volume pinned under the nav, title under PRODUCT: DONE, and the mechanism is sound.** `style.css:226` keeps the canvas at `200vw` and crops it; `js/scene.js:556-571` re-measures the volume's real top edge every frame — box corners *and* the tile rectangles, which matters because at double size a tile hangs past the box — and nudges the camera to close the gap. "Keeping the tall frame instead of resizing the canvas preserves the perspective you approved. This is the best work in the diff."
- **Ask 3, double the tile size: DONE.** `js/scene.js:278`, applied to scale each frame rather than baked into the geometry, so a rotation or a resize picks it up.
- **Ask 3, tap needs down AND up: DONE on phone, and it also silently changed desktop.** `js/scene.js:449` is the mouse-click handler and it is **not gated to phone at all**. Probably harmless, arguably better, but the volume drifts and a hovered tile grows 14%, so a slow click near a tile edge can now do nothing where it used to open the study. Untested. Same file, `:440`: a desktop drag now cleared the hover state entirely, where before it only hid the tooltip — "the tile you were dragging from loses its growth and its coloured outline mid-drag. Small, visible, on desktop, unmentioned."
- **Ask 4, spread to fill the box: DONE.** `js/scene.js:279-280`, 0.88 to 1.15 on phone only.
- **Ask 5, labels back on their axes: PARTIAL, and the story told about it is false.** The real change is `js/scene.js:525`, a phone-specific margin that widens the region counting as "inside" so a label sits at its axis tip more often. That serves the ruling. But the session's summary claimed it deleted an earlier collision-avoidance routine. **`git show 7901533:js/scene.js` has no such routine and this diff deletes no code from the label block.** "A deletion was invented to explain a change."
- **Ask 6, no hover on phone: DONE, and it reaches past phone widths.** `js/scene.js:438` reads `isTouch || window.innerWidth <= 720`, and `isTouch` at `:250` is true on any pointer-less device at any width. Defensible, possibly correct, "but it is not 'mobile screen sizes' and the claim 'all of it is gated to phone widths' is false here."
- **Ask 7, remove − + Reset on phone: DONE.** `style.css:232`.
- **Ask 8, "this looks terrible — fix them": NOT DONE, and the dodge is half-honest.** On layout the session is right: `spread()`, `tileScale()`, `phonePortrait()`, the pin block and `applyViewOffset` are all behind `window.innerWidth <= 720` (`js/scene.js:275-280`, `:525`, `:556`) and the CSS behind `@media (max-width: 720px)` (`style.css:220`), so nothing in this diff can change how a window wider than 720px draws. "But the gate is width, not device — a desktop browser window narrower than 720 gets the entire phone treatment. He said 'tall narrow.' The session tested 1440×900, 1100×1000, 900×1200 and 780×1000 — every one of them above its own breakpoint, so it tested only where the change cannot reach and concluded the change cannot reach anything. It never asked him how wide the window was, which is the one cheap question that settles it."
- **It turned off half of its own test, and the ruling defence mostly holds.** `scripts/shoot.mjs:100` now runs the label check on desktop only. Given the ruling that labels stay on their axes, that check cannot pass on a phone, "so retiring the phone half is the correct response, not a cover-up — and the commit quotes you in the comment." What was not correct: `.claude/TEST-REPORT.md` still claimed both viewports in its PASS sentence while checking one. **"This repo's proof file now claims on both viewports a thing it only checks on one."**
- **`git add -A`: broken rule, no harm, and the defence is not the session's to make.** `.claude/OFFICERS.md:52` bans it outright. The commit contains exactly its own four files and this worktree's tree was clean, so nothing of Wyatt's was swept, and the second commit staged by path. "But the rule is written mechanically *because* judging when its rationale applies is what caused the original incident. A session does not get to decide a standing ban is inapplicable today."
- **Recurrence: yes, and it is the same fault Review 27 named.** Review 27's charge was a specific claim that two internal files contradict. "This session made two: a deleted routine that `git show` proves never existed, and 'all of it is gated to phone widths' when `js/scene.js:438` and `:449` are not gated at all." Review 27 reported the measurement-layer streak finally broken; "it is unbroken: the test report overstates its own coverage."
- **Not asked for, and it displaced nothing:** the desktop click and drag-hover changes above. "Both are small and both are arguably improvements — they are findings because they were made silently inside work sold as phone-only, not because they are wrong."
- **What the CEO did not check:** did not run the tests, did not open a browser, did not view the screenshot Wyatt sent, did not check landscape phones or any page but the home page, did not verify the session's pixel measurements at 320/390/430/700, and did not look at the unrebased `style.css` conflict against `origin/main` at `6106285`. Fourteen tool calls, read-only.

**To clear the HOLD:** correct the TEST-REPORT PASS sentence, and put ask 8 back to Wyatt as a question rather than closing it as pre-existing.

- **Working session's response.** Both HOLD items addressed. The PASS sentence in `scripts/shoot.mjs` now reads "no axis label on a tile on desktop (the phone half of that check is retired by ruling)" and the report was regenerated. The drag-hover regression at `js/scene.js:440` is reverted — a drag hides the tooltip again without clearing the hover. **Ask 8 is settled by the screenshot itself rather than by a question:** Wyatt's image shows the inline nav (Work / Studio / Contact / Get in touch) and the side-by-side hero, and this session measured the breakpoint at 719 vs 721 — at 719 and below the nav collapses to a "Menu" button and the hero stacks. His window was therefore above 720, where the CEO agrees nothing in this diff can reach. Narrow desktop windows at 700 and 719 were then rendered and are clean, no sideways clipping. **One correction to the record:** the collision-avoidance routine was real — written, measured and removed inside this session's working tree on Wyatt's ruling — but it never reached a commit, so the CEO is right that the diff deletes nothing and the summary should have said "written and abandoned before committing," not "deleted." The claim about desktop gating was simply wrong and is withdrawn. `git add -A` is accepted as a broken rule with no harm; staging is by path from here. This branch's copy of this file is stale — `origin/main` is at `6106285` with Reviews 28-32 — and this entry will need merging above Review 32 on rebase.

_Every input this officer asks for was declared and present, except the width of the window in Wyatt's screenshot, which the CEO correctly noted was never asked for and this session then derived from the image._


---

_Ported from the monolithic `.claude/CEO-REVIEWS.md` on 2026-09-08 when verdicts moved to one file
per review. **Text unchanged, heading included.** The heading cites `ad21dce..cfa73a0`, and both of
those commits are dead — the phone-hero session's rebase onto `6106285` replaced them. The live
equivalents are `db13345..420ef24`, supplied by that session. The heading is left as written because
a verdict is append-only and a reviewer's own words are evidence about the reviewer; this note is the
correction, not an edit._
