# hycu backlog

Item ids are `HY-<n>`. Each needs a reviewed commit to ship.

- ~~HY-1~~ done 2026-09-02 (pointer-events none on desktop only).
- **HY-1 (was)** `.hero-copy { pointer-events: none }` should apply on desktop only; on the phone the copy sits below the graph and the rule only blocks text selection. (CEO Review 4)
- ~~HY-2~~ done 2026-09-02 (browser pass fails if the brand's left edge is under 24px).
- **HY-2 (was)** Browser pass: also fail if the nav brand's left edge is under 24px, so the gutter test measures amount as well as alignment. (CEO Review 4)
- ~~HY-3~~ done 2026-09-08. The browser pass runs at 820px now, and the headline that rendered six lines at 721 and five at 768 renders three from 480 up. Open since CEO Review 4, 2026-09-02.
- **HY-3 (was)** Test a 820px-wide viewport: between 720 and 900px the headline overlays a graph that is not shifted right. (CEO Review 4)
- **HY-4** `--edge` uses `100vw`, which includes the scrollbar on Windows; consider `100%` of a wrapper instead. (CEO Review 4)
- ~~HY-5~~ Formspree wired 2026-09-02 (maeyjowq). Calendly removed by ruling the same day.
- ~~HY-6~~ done 2026-09-02 (certificate approved, Enforce HTTPS on). Was: Enable HTTPS enforcement once GitHub issues the certificate (background poll running 2026-09-02).
- ~~HY-7~~ done 2026-09-02 (Three.js r160 vendored to `vendor/`). Was: Vendor Three.js r160 into `vendor/` instead of loading from jsdelivr: Wyatt's uBlock blanked the graph on 2026-09-02; any visitor with that list sees an empty hero. (session)
- ~~HY-8~~ done 2026-09-02 (dead aria-labelledby removed; browser validation on). Was: `contact/index.html`: remove the dead `aria-labelledby="write-title"`; drop `novalidate` or add JS validation so `required` means something. (CEO Review 7)
- ~~HY-9~~ done 2026-09-02 (Calendly mentions gone from EDITING.md and CTO-QUESTIONS.md). Was: EDITING.md:16 and `.claude/CTO-QUESTIONS.md` still mention Calendly. (CEO Review 7)
- ~~HY-10~~ done 2026-09-02 (label check also sampled after a hard drag, desktop). Was: Label-on-tile check samples only the resting view; also sample a dragged view near the 55° limit. (CEO Review 7)
- ~~HY-11~~ done 2026-09-02 (Origin/Host check, path boundary). Was: `scripts/edit-server.mjs`: require `Origin`/`Host` of 127.0.0.1:8788 on `/__save`; `fileFor` must compare against `ROOT + path.sep`. (CEO Review 8)
- ~~HY-12~~ done 2026-09-02 (`scripts/edit-test.mjs`, `npm run edit-test`, writes PASS/FAIL to TEST-REPORT.md). Was: An editor test in the repo (`scripts/edit-test.mjs`, run by `npm test` only when the editor is up), and red-proofs that leave a trace in TEST-REPORT.md rather than the terminal. (CEO Review 8)
- **HY-18** Editor: drag-select inside the home work cards (they are `<a>` elements; browsers drag links rather than select their text). Untested. (CEO Review 9)
- **HY-13** Label check: sample the other orbit side, an upward tilt, and a phone drag. (CEO Review 8)
- **HY-14** Consider vendoring Geist / Geist Mono like Three.js; today a blocked font falls back to Helvetica. (CEO Review 8)
- **HY-15** Fix the "whatever the angle" comment in `js/scene.js` (placement is safe; the margin clamp was the fault). (CEO Review 8)
- ~~HY-16~~ done 2026-09-02 (submit blocked while editing). Was: In editing mode, intercept the contact form's submit so a stray click on Send does not mail a test through Formspree. (CEO Review 8)
- ~~HY-17~~ done 2026-09-02: hero copy is unselectable/uneditable in the editor because the site passes clicks through it to the graph; the editor now overrides that. (Wyatt)
- **HY-19** Editor test: exercise the refusal branch (page/file count disagreement) and red-proof the meta-tag case against the substring matcher. (CEO Review 11)
- **HY-20** Editor: remove `data-failed` on the next successful edit or after a delay; today one failed save leaves the element unmatchable until reload (fails safe). (CEO Review 11)
- **HY-21** Editor test: cover the trailing-nbsp strip and the caret collapse. (CEO Review 11)
- **HY-22** `npm install` Playwright in this repo so `scripts/*.mjs` stop falling back to the portfolio's node_modules. (CEO Review 11)
- **HY-23** `js/scene.js`: remove the dead `C.bg` constant and its invented comment. (CEO Review 13)
- **HY-24** Home graph: ClaudeKit's bottom edge still tucks under Polycam's near tile; either move it again or accept, and stop describing it as "clear". (CEO Review 18)
- **HY-25** Rails: Pour says "not named", oral care says "unnamed by agreement" (a ruling exists for oral care). Wyatt to choose one form. (CEO Review 18)
- **HY-26** ClaudeKit, two changes that land together the day the repo goes public: add the line saying it is public, and raise the tile's `reach` from 0.25 back to 0.8. Wyatt approved the line 2026-09-03; `gh repo view wyattroy/claude-kit` returned PRIVATE the same day ("too entangled with pastry pirates at the moment... the intention is to make it public SOON"). The 0.8 shipped in `fa3de7c` and was reverted by CEO Review 22: the axis is reach, one person to a public, and a private repo is one person. Verify with `gh` before writing either. **Wyatt ruled 2026-09-03: leave the 0.25 as it is — "claude-kit needs serious work before it can be made public." This item is parked, not pending; do not raise it again until he says the repo is opening.** (CEO Review 22)
- **HY-27** `.cap-bar` alignment below 900px: the comment at `style.css:256-259` says each segment sits at exactly its column's width, but `.caps` drops to two columns at 900px and one at 520px (`style.css:249-250`) while `.cap-bar` stays four (`style.css:261`). On the studio page the bar sits under a `.not-list` of stacked rows (`style.css:347`), so it aligns to nothing there at any width; `style.css:290` concedes this by tightening the gap. Aria-hidden decoration, reads fine — fix the CSS or fix the comment. (CEO Review 23)
- **HY-28** The forgiveness-platform proposal is the register Wyatt named as the standard ("the forgiveness-platform proposal is the best example of this"), and two consecutive voice passes have now been written without it — it is not in this repo and the password must not be automated. Before any further voice work, get that document readable by the session doing it. Second half, from the same review: the demonstrative-opener count of 0 was measured with a pattern matching only "That/This/Those + to be"; the honest site-wide count is 2 — `work/spatial-equity/index.html:51` "That looks like" and `work/oral-care-research/index.html:51` "That reframed". Both were judged worth keeping. Measure with the pattern that matches the tic. (CEO Review 25)
- ~~HY-29~~ answered 2026-09-03 by Wyatt: `index.html:101` stays **"every project in this studio"**. Review 23 asked whether the claim should narrow back to "on this page", since "in this studio" covers work the repo cannot check. His answer: "in this studio". Closed, no change made. (CEO Review 23)
- **HY-30** `data/projects.json` polycam `reach` 0.7 back to **0.5**, Wyatt's brief's own number. CEO Review 26: the raise leans on the 2x citation figure that `work/polycam/index.html:75` itself calls an early signal rather than a settled result, and 0.7 places six scripts on a client's YouTube channel above a published book (0.66) and a shipped public game (Pastry Pirates, 0.48). "A number you hedge in prose is not a number you may lean on in data." (CEO Review 26)
- **HY-31** Two demonstrative openers on the new Polycam page, `work/polycam/index.html:50` "That turned out not to fight..." and `:52` "That matters for citation...". Site-wide honest count is now 4, up from the 2 recorded in HY-28. The brief that shipped them measured sentence length, contractions and em-dashes and did not measure the tic the backlog names. Fix the two sentences, and put the opener grep in the measurement set so this stops being found by the reviewer. (CEO Review 26)
- **HY-32** `355f7f9` swept one of Wyatt's own uncommitted edits into a session commit: `studio/index.html:69`, where he had replaced the local-speech-model sentence with "A team hierarchy of agents that hold each other accountable." `git add studio/index.html` staged the whole file instead of the session's hunks. This is the CEO Review 9 fault recurring, on the same day the session wrote the rule against it into EDITING.md. Nothing false shipped and the CEO judged the new sentence supportable, but it traded a specific checkable claim for a vaguer one and it went out undeclared. Wyatt to say whether he wants the local-speech-model sentence back. (CEO Review 26)
- ~~HY-33~~ closed 2026-09-08 by Wyatt: "I don't care about HY33 -- don't use CTO, it's unnecessary." The CTO officer is not used on this repo, so the studio sentence is being read as describing the officer system generally rather than this site's build. Wyatt declined the change through six reviews; recorded as his ruling rather than an open item.
- **HY-33 (was)** `studio/index.html:69` claims "two officers whose only work is to judge the rest, which is the pipeline that built this site." Two files in this repo contradict it: `.claude/CTO-LEDGER.md:3` says no CTO has ever driven this repo and the site was built in an interactive session, and `.claude/OFFICERS.md:30` says a CTO here has no output channel. One officer judged this site, and `work/claude-kit/index.html:35` already gets this right by narrowing to "an officer's review". CEO Review 27's replacement wording, verified: "...two officers whose only work is to judge the rest, and an officer's review gated every push that built this site." Review 27 passed it as one clause inheriting from an already-approved page and said **it becomes a blocker at the next review**. First item of the voice-pass session. (CEO Review 27)
- ~~HY-34~~ closed 2026-09-08 by Wyatt: "font looks good, ship it." The webfont ships unmeasured on all thirteen pages by his decision. No transfer-size, FOUT or CLS number exists; four CEOs asked for one and none could take it, being forbidden a browser.
- **HY-34 (was)** The Shrikhand webfont is unmeasured. Five pages now load a display face on top of Geist and Geist Mono, and nobody has weighed the transfer size, the FOUT with `display=swap`, or the layout shift when a 58px headline repaints from the grotesk fallback into a much wider face. Three consecutive CEOs said the same thing: "nobody has measured transfer size, FOUT or layout shift, and that remains completely unmeasured." `scripts/shoot.mjs` already drives a headless browser at both widths and is the obvious place to add a CLS and a transfer-size check. (CEO Reviews 28 and 29)
- **HY-35** `254591c` was staged with `git add -A`, which `.claude/OFFICERS.md` bans by name from the Review 9 incident. Wyatt's tree was clean at the time so nothing of his was swept, and the following commit staged by path — but the rule was broken without being noticed, by a session that had not read OFFICERS.md until the CEO step. The ban lives in the officer adapter, which a working session has no reason to open. Consider moving it somewhere a session reads before its first commit: CLAUDE.md, or a pre-commit hook that refuses `-A`. (CEO Review 28)
- ~~HY-36~~ done 2026-09-08. Wyatt: "every project should have ONE name and ONE byline, consistent." All four name slots per project now carry the same string (graph tile, page <title>, Work card, study head), the next-study links use it too, and the client and capability match between card, study head and projects.json. One rename in the doing: the field guide went to "How to Change Institutions", its published title, over the shorter "Changing Institutions" — a real name beats an invented short one, and its own lede already used it.
- **HY-36 (was)** A project can be named three different ways in three places, and the Polycam study now is: `<title>` says "Polycam", the graph tile (`data/projects.json` `name`, drawn by `js/scene.js:147`) says "Written to be cited", and the new `.proj` title on both the Work card and the study head says "Cited by AI". Five of the eight new titles differ from their tile: Spatial Equity, What They're Buying, Cited by AI, Teaching Forgiveness, Changing Institutions. CEO Review 30 said the titles are good and would keep every one — the question is whether the graph tile names and the `<title>`s should be brought to match them, or whether three registers is deliberate (tab = client, tile = the finding, page = the project). Wyatt's call; nothing is wrong until he rules. (CEO Review 30)
- **HY-37** The eyebrow's capability and `data/projects.json` `capabilities` disagree for four of eight projects, and it is visible: `js/scene.js:54` colours each graph tile by `capabilities[0]`, so Spatial Equity reads "Design research" on its card and study head while its tile is coloured User research. Same for Polycam (page "Content strategy", tile Strategy), forgiveness (page "Strategy & platform", tile Strategy) and Pastry Pirates (page "Game & systems design", tile Systems design). These may be two different fields on purpose — one descriptor for a reader, a list for the graph — or the page words may want to become the first capability. Wyatt's call; nothing is false either way. (CEO Review 32)
- **HY-38** The study rail is a fifth place a byline appears and it is inconsistent in two ways. The field is labelled **For** on `work/spatial-equity/index.html:76` and `work/pour/index.html:72` and **Client** on the other six. And where the card and `projects.json` say "Studio &middot; Hycu", the rail says "Studio project" (`work/claude-kit/index.html:71`, `work/pastry-pirates/index.html:76`, `work/how-to-change-institutions/index.html:70`). The rail is deliberately longer and more specific than the eyebrow, so this may be right as it stands — but "For" on exactly the two pages with no paying client looks like a real distinction that should either be the rule or go. (CEO Review 32)
- ~~HY-39~~ closed 2026-09-09 — the gate is deleted, so the exemption that could remove it is moot. CI cannot be exempted by a path.
- **HY-39 (was)** `scripts/hooks/pre-push` exempts `^scripts/hooks/` from its own gate, so a session can weaken or delete the gate and ship that change to `main` with nobody reading it. CEO Review 35: "the comment says this is deliberate; it is still the one exemption that can remove the gate." It is pre-existing, and it is the exemption the 2026-09-08 hook fixes themselves shipped under. Options: drop the exemption and accept that hook changes need a verdict; or keep it and add a check elsewhere (a CI job that diffs the hook, or branch protection) so a gate change cannot be invisible. Not urgent while Wyatt is the only person pushing; it is the kind of hole that stops being funny with a second contributor. (CEO Review 35)

- **HY-40** `npm run serve` starts a server on the fixed port 8787 from whatever directory it is run in, and it outlived the session that started it by eleven hours. `scripts/shoot.mjs` no longer trusts that port, but nothing stops the next person being confused by a stale server answering it, and `EDITING.md` still tells Wyatt to run `python3 -m http.server 8000` by hand. Consider making `npm run serve` print its root, or bind a port derived from the working tree so two worktrees cannot collide on one. (session, 2026-09-08)
- ~~HY-41~~ closed 2026-09-09 — the gate is deleted, so nothing re-blocks a push after a verdict. CI grades the whole tree and does not care which lines are comments.
- **HY-41 (was)** `scripts/hooks/pre-push` decides what needs a verdict by file PATH, so a comment added to `style.css` is as blocking as a rewrite of it. On 2026-09-08 four separate post-verdict commits — each one applying fixes the reviewing officer had itself demanded — re-blocked the push and asked for another review; the fourth was two comments and a dead ternary, and Wyatt authorized a bypass rather than a fourth review of one branch. Worth exploring: ignore comment-only and whitespace-only hunks when computing the unreviewed set (`git diff -G'^[^ \t]*[^ \t/*]'` gets close for CSS/JS), or let a verdict name a range it covers rather than a single sha. Do not weaken it to "trust the session's judgement" — the point is that the session is the one thing that cannot review itself. (Wyatt, 2026-09-08)
- **HY-42** `core.hooksPath` had been set per-worktree in `.git/worktrees/<name>/config.worktree` on two worktrees only, never in the main checkout and never in `hycu-cubes`. So "every push is gated" was true of two working copies and Wyatt's own pushes were never gated at all. Nothing to fix — the gate is gone — but every CEO verdict before 2026-09-09 that reasons about "every push" was reasoning about a smaller set than it thought, and `.claude/OFFICERS.md` now carries that note. Worth remembering if the gate is ever rebuilt: set it once, in a place that applies everywhere, or it is not a gate. (session, 2026-09-09)
- ~~HY-43~~ done 2026-09-09. Real tracking replaces the injected spaces, the client name wraps to at most two lines, and it shrinks only if two lines still overflow — which none of the eight do. Measured in a browser with the real Geist Mono: all eight fit at full size, four on one line and four on two.
- **HY-43 (was)**  Four of the eight tile eyebrows overflow the face of their cube. The client name is drawn across the top of each tile with no wrapping and no clipping, so it runs off the edge. Measured in a browser with the real Geist Mono against 912px of available width: "A residential eating-disorder facility" 1650px, "Human Flourishing Program, Harvard" 1474px, "Harvard Graduate School of Design" 1430px, "A consumer oral-care brand" 1122px. **This predates the cubes** — identical before and after that branch; the larger faces only make it easier to see. Options: wrap to two lines, clip with an ellipsis, shrink the eyebrow, or give `data/projects.json` a short display name for the tile. The last is a copy decision and Wyatt's. **Cause confirmed in code 2026-09-09:** `js/scene.js:177` draws the eyebrow with a bare `ctx.fillText(eyebrow, padX, y)` and no width argument, while the project name eleven lines below it at `:185` uses `wrapLines(ctx, project.name, FACE_W - padX * 2)`. The eyebrow is also letter-spaced by `project.client.toUpperCase().split('').join(' ')` at `:176`, which roughly doubles its width — so it is the one string on the face that is both widened and unconstrained. The smallest fix that keeps his copy is to pass the eyebrow through the same `wrapLines` the name already uses, or shrink its font until it fits; neither needs a ruling. (cloud session + local confirmation, 2026-09-09)
- **HY-44** `.claude/HANDOFF-GRAPH-CUBES.md` section 5 reports that `npm test` fails on `origin/main` at 820px, the hero headline breaking into four lines instead of three. **It does not reproduce**, checked twice on 2026-09-09: the GitHub Actions run on `main` at `e642098` is green, and running the browser pass on a Mac with the real webfonts is green at all three widths. The likely cause is the one that handoff documents itself in section 6 — the cloud container's proxy blocked `fonts.googleapis.com`, so text was measured in a fallback face. Nothing to fix; recorded so the claim is not inherited as fact by the next session that reads the handoff. (session, 2026-09-09)
